<h1>Edit Unit</h1>
<%= form_with(model: @unit, local: true) do |form| %>
  <div>
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :active %>
    <%= form.check_box :active %>
  </div>

  <%= form.submit %>
<% end %>
